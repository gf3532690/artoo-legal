# Web 框架
fastapi>=0.115.0
uvicorn>=0.30.6
# WebSocket 后端：uvicorn 需要它才能接受 WS 握手（会话上传状态实时推送 /api/sessions/{id}/files/events）
websockets>=12.0
pydantic>=2.12.0
pydantic-settings>=2.5.2
python-dotenv>=1.0.1

# 数据库
sqlalchemy>=2.0.35
asyncpg>=0.29.0
aiosqlite>=0.20.0
greenlet>=3.0.0
pymilvus>=2.5.0

# Redis
redis>=5.0.0

# 对象存储（MinIO / S3 兼容，知识库源文件权威存储）
minio>=7.2.0

# HTTP & API
httpx>=0.27.2
python-multipart>=0.0.12
sse-starlette>=2.1.3

# YAML 解析（Agent Skills 加载）
PyYAML>=6.0

# Token 计算（Agent 上下文估算）
tiktoken>=0.7.0

 # 文档解析
 pymupdf>=1.25.0
 python-docx>=1.1.2
 # docx 段落文本抽取直接依赖 lxml 文档树（python-docx 也依赖它，这里显式声明，
 # 不靠传递依赖——见 app/pipeline/loaders/docx_xml_text.py）
 lxml>=5.0.0
 openpyxl>=3.1.5
 python-pptx>=1.0.2

# 网页正文提取（链接转存：抓取网页 / 微信公众号图文链接，提取正文为 Markdown）
trafilatura>=1.12.0

# 中文分词（MMR 去冗余的词级 Jaccard；缺失时退化字符 bigram，不强依赖）
jieba>=0.42.1

# ML 模型依赖已移除，Embedding/Rerank 统一使用远程服务

# OCR：统一使用远程 API（external_api / textin）
# httpx 已在上方 HTTP & API 部分引入，OCR Provider 直接复用

# 认证与授权（tenant-auth）
bcrypt>=4.1          # 口令哈希（自带 salt + 工作因子，纯 C 跨平台 wheel）
PyJWT>=2.9           # JWT 签发/校验（HS256）
# 第三方凭据加密存储（AES-256-GCM）。用于 outbound MCP server 的静态凭据：
# 这类 token 必须能取回原文发给对方，无法像 AK/SK 那样"派生而不落库"，故加密落库。
cryptography>=43.0

# 系统资源检测（session-file-upload：单库 chunk 上限内存推荐，cgroup 不可用时回退物理内存）
psutil>=5.9

# 测试
pytest>=8.3.3
pytest-asyncio>=0.24.0
nest-asyncio>=1.6.0
hypothesis>=6.112.1
fakeredis>=2.35.1    # Redis Stream 队列测试替身（TaskQueue / 会话上传队列 fakeredis 回归基线）
